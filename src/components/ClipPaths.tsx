import { IMAGE_PLACEHOLDER_ID } from '@components/gallery/sections/constants';
import { ImagePlaceholder } from './icons/ImagePlaceholder';

export const ClipPaths = () => {
  return <ImagePlaceholder id={IMAGE_PLACEHOLDER_ID} />;
};
