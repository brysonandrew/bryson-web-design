import { TPricingKey } from "@pages/pricing/config/types";

export const BG: Record<TPricingKey, string> = {
  standard: 'bg-standard gradient-standard',
  plus: 'bg-plus gradient-plus',
  select: 'bg-select gradient-select',
};
