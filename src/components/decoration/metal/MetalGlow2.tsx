import type { FC } from 'react';
import { MetalGlow } from './MetalGlow';

export const MetalGlow2: FC = () => {
  return (
    <MetalGlow
      classValue='rounded-md'
      color='accent'
      drop={1}
    />
  );
};
