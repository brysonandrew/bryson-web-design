import type { FC } from "react";
import { IconsSvg, TIconsSvgProps } from "@brysonandrew/svg/icon";

export const Icon40: FC<
  TIconsSvgProps
> = (props) => {
  return (
    <IconsSvg
      size={40}
      {...props}
    />
  );
};
