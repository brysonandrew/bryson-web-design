import type { FC } from "react";
import { IconsSvg, TIconsSvgProps } from "@brysonandrew/svg-icon";

export const Icon28: FC<
  TIconsSvgProps
> = (props) => {
  return (
    <IconsSvg
      size={28}
      {...props}
    />
  );
};
