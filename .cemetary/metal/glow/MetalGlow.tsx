import { TColorKey } from '@app/colors/types';
import { useDarkMode } from '@lib/hooks/dark-mode/useDarkMode';
import type { FC } from 'react';
// import { Glow, TElementProps } from '.';

// type TProps = TElementProps;
// export const MetalGlow: FC<TProps> = (props) => {
//   const { isDarkMode } = useDarkMode();
//   const color = (
//     isDarkMode ? 'accent' : 'secondary'
//   ) as TColorKey;

//   return <Glow color={color} {...props} />;
// };
