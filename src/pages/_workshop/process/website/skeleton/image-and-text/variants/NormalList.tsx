import { TextList } from '../TextList';
import { Image } from '../Image';
import { Shell } from '../Shell';

export const NormalList = () => {
  return (
    <Shell>
      <Image />
      <TextList />
    </Shell>
  );
};
